import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '01-app-ionic',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
